# Tracklist Sources

Generated by scanning music-sites output files for tracklist/playlist/chart content patterns.

## 发现历史

### 2026-05-07 第一轮扫描

**Fluid Radio** — `fluid_radio_reviews.json`
- 671 条存档，Soundcloud/bandcamp 嵌入 player 内容
- 类型：tracklist（radio session / session recordings）
- 特点：声景/环境/实验方向，质量高但都是 2013-2022 老内容
- 处理：`crawl_strategy: skip`，推荐时从存档随机抽 2-3 条

**The Wire** — `/audio/tracks`
- 每篇帖子的 `Tracklist:` 段，含 `Artist "Track Title"` 或 `Artist *Album Title*` 格式
- 类型：tracklist
- 处理：scraper 从 `Tracklist:` 段提取艺人+曲名/专辑名作为曲目列表

## 待扫描站

Pipeline 跑完后应扫描以下站的 output 文件，搜 `Tracklist:`、`Track: `、`01.`、`— Artist` 等 pattern：

- Mixmag Asia（guest mix）
- Resident Advisor（charts）
- Bandcamp Daily（playlists）
- Point of Departure（录音室会话）

发现后追加到此文档，并更新 sites.json 对应站点的 `notes` 字段。
